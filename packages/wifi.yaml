# packages/wifi.yaml
# Shared WiFi configuration with captive-portal fallback.

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  power_save_mode: none
  ap:
    ssid: "${device_name} Fallback AP"
    password: !secret fallback_ap_password

captive_portal:
